.page-top.animated.fadeInDown
  .nav
    li
      if is_home()
        a.current(href=config.root)= __('Home')
      else
        a(href=config.root)= __('Home')
    li
      if is_archive()
        a.current(href=config.url + "/archives")= __('Archive')
      else
        a(href=config.url + "/archives")= __('Archive')
    //- li
      //-
        if is_current('about')
          a.current(href="/about")= __('About')
        else
      a(href="/resume",target="_blank")= __('About')
    //-
      li
        if is_current('links')
          a.current(href="/links")= __('Links')
        else
          a(href="/links")= __('Links')
  .information
    .back_btn
      li
        if is_post()
          a.fa.fa-chevron-left(onclick="window.history.go(-1)")
        else
          a.fa.fa-chevron-left(onclick="window.history.go(-1)",style="display:none;")
    //-
      .avatar
        -img(src=theme.avatar)
